import 'package:flutter/material.dart';

class GridCategorias extends StatelessWidget {
  const GridCategorias({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
